// Overwritten with the deployed commit SHA by the Vercel install command.
// "dev" locally. Client and server compile from the same file, so a version
// mismatch against /api/version means a newer deployment exists.
export const APP_VERSION: string = "dev";
