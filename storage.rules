rules_version = '2';

// PDF originals live at materials/<uid>/<materialId>.pdf and are private to
// the owner.
service firebase.storage {
  match /b/{bucket}/o {
    match /materials/{uid}/{file=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
